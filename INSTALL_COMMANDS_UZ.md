# CyberShield Windows commands

After installing the user PATH entries and opening a new terminal:

- `cibershield` — main desktop application.
- `cibershield terminal` — defensive security terminal.
- `ciber` — Claude-like CyberShield AI operator terminal.

`python main.py` remains supported as a compatibility launcher; the new named command is `cibershield`.
